name: CI Analysis Service

on:
  pull_request:
    branches:
      - main
    paths:
      - "analysis/**"
      - ".github/workflows/ci-analysis.yml"

permissions:
  contents: read

jobs:
  test-analysis:
    runs-on: ubuntu-24.04

    defaults:
      run:
        working-directory: analysis

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v9
        with:
          enable-cache: true
          cache-dependency-glob: "analysis/uv.lock"

      - name: Install Project Dependencies
        run: |
          uv sync --frozen

      - name: Run Test Suite
        run: |
          uv run pytest -v -m "not e2e"

      - name: Build Analysis Docker Image
        run: |
          docker build \
            --tag resumeranker-analysis-ci:pr-${{ github.event.pull_request.number }} \
            .

      - name: Verify Docker Image
        run: |
          docker image inspect \
            "resumeranker-analysis-ci:pr-${{ github.event.pull_request.number }}" \
            > /dev/null

          echo "Analysis Docker image built successfully."
