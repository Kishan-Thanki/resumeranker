// Stub for SvelteKit's `$app/environment` used only during Vitest runs.
// Tests can still override individual values via `vi.mock('$app/environment')`.
export const browser = true;
export const dev = true;
export const building = false;
export const version = 'test';
