// Click-wrap policy version the user is agreeing to when they accept the
// Terms of Service and Privacy Policy. Must match the
// `current_policy_version` constant in `backend/app/config.py` and the
// `lastUpdated` date shown on /terms and /privacy. Bump all of those
// together when either document materially changes.
export const CURRENT_POLICY_VERSION = '2026-05-19';
